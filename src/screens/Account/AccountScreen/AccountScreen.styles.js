import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabIndicator: {
    backgroundColor: "#fff",
  },
  tabItemContainer: {
    backgroundColor: "transparent",
  },
});
