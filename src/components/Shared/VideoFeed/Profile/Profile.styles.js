import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {
    alignItems: "center",
    marginBottom: 20,
  },
  avatar: {
    borderWidth: 1,
    borderColor: "#fff",
  },
  iconContainer: {
    position: "absolute",
    bottom: -7,
    backgroundColor: "#d40000",
    borderRadius: 100,
    padding: 1,
  },
});
